%header
  .inner
    %a.logo{:href => root_path} Persona
    - if user_signed_in?
      = link_to('Sign out', destroy_user_session_path, :method => :delete, class: "user_link")
    - else
      = link_to "Sign in", new_user_session_path, class: "user_link"




