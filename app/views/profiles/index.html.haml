.intro
  %h1

.main
  .wide-container
    %a{:href => new_profile_path}
      .profile.add
        %h2 Add new profile
    .clear

    %h2 Random set
    - @profiles.each do |profile|
      %a{:href => profile_path(profile)}
        .profile
          .image
          %h2=profile.name
          .basics
            = profile.age
            = profile.occupation

          %p= profile.description
          %p= profile.living_situation
          %p= profile.interests
          %p= profile.quote
          %p= profile.ability
          %p= profile.aptitude
          %p= profile.attitude
          %p= profile.bigger_picture
          %p= profile.considerations
          %p.pack= profile.pack

    %h2 Packs


    %h2 Your deck

